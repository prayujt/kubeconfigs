apiVersion: v1
kind: ServiceAccount
metadata:
  name: subsonic-streaming-service-account
  namespace: subsonic-streaming
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: subsonic-streaming
  name: job-manager
rules:
- apiGroups: ["batch"]
  resources: ["jobs"]
  verbs: ["create", "get", "list", "watch", "delete", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: job-manager-binding
  namespace: subsonic-streaming
subjects:
- kind: ServiceAccount
  name: subsonic-streaming-service-account
  namespace: subsonic-streaming
roleRef:
  kind: Role
  name: job-manager
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: subsonic-streaming
  namespace: subsonic-streaming
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subsonic-streaming
  template:
    metadata:
      labels:
        app: subsonic-streaming
    spec:
      serviceAccountName: subsonic-streaming-service-account
      containers:
      - name: subsonic-streaming
        image: docker.prayujt.com/distributed-streaming-api
        ports:
        - containerPort: 8080
        envFrom:
        - secretRef:
            name: subsonic-streaming-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: subsonic-streaming
  namespace: subsonic-streaming
spec:
  type: ClusterIP
  selector:
    app: subsonic-streaming
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080

